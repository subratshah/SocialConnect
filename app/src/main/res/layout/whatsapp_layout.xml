<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/whatsapp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="WHATSAPP"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/whatsapp_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher_background"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/whatsapp" />

    <TextView
        android:id="@+id/whatsappName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="WHATSAPP NAME"
        app:layout_constraintBottom_toTopOf="@+id/whatsappLast"
        app:layout_constraintStart_toStartOf="@+id/whatsapp"
        app:layout_constraintTop_toBottomOf="@+id/whatsapp" />

    <TextView
        android:id="@+id/whatsappId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="40dp"
        android:text="WHATSAPP STATUS"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@+id/whatsappName"
        app:layout_constraintStart_toEndOf="@+id/whatsappName" />


    <TextView
        android:id="@+id/whatsappLast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="WHATSAPP STATUS"
        app:layout_constraintBottom_toBottomOf="@+id/whatsapp_image"
        app:layout_constraintStart_toStartOf="@+id/whatsapp" />

</androidx.constraintlayout.widget.ConstraintLayout>